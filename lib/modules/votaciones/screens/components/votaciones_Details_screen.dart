import 'package:flutter/material.dart';

class VotacionesDetailsScreen extends StatelessWidget {
  const VotacionesDetailsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('VotacionesDetailsScreen'),
      ),
    );
  }
}
